# Tracking Plan: {product or AI feature name}

<!--
Generated by the Actuals instrument skill from metrics/MEASUREMENT.md v{X.Y.Z}.
This file is the source of truth for what gets tracked — the typed constants
module in events/ is generated FROM it. Add a row here first, then regenerate.

Fill rules:
- Maps-to-metric is MANDATORY: every row names the spec metric(s) it feeds
  (OM-x / GM-x). An event no metric consumes is deleted — the anti-bloat rule
  (Actuals catalog VM-20). Deletion is a feature: unconsumed events are where
  definition rot and PII risk accumulate.
- Event names: object_action, snake_case, past tense (ticket_deflected). A
  breaking change to an event's meaning or trigger gets a NEW versioned name
  (ticket_deflected_v2) — never a silent redefinition (VM-18).
- One event per user action — two events for one action double-counts
  everything downstream (VM-07).
- Properties: name:type, snake_case. Types: string | number | boolean |
  enum(...) | timestamp. Group mentally as actor / object / context; prefer
  enum over free text. No raw message/prompt content, no plain emails or
  names — hashed IDs only. A property no formula or filter consumes is not
  collected.
- Trigger point: name the exact code-path moment ("at resolution commit"),
  not the feature area ("in the assistant"). Vague triggers produce double
  fires and double counts (VM-07).
- Owner: a named human, never a team and never "TBD" (VM-20).
- Status: planned | staged | live | deprecated. Deprecated rows carry a
  delete-by date in the trigger-point column until removed to the graveyard.
-->

- **Spec:** metrics/MEASUREMENT.md v{X.Y.Z}
- **Last-Updated:** {YYYY-MM-DD}

## Events

| Event | Trigger point | Properties (name:type) | Owner | Maps-to-metric | Status |
|---|---|---|---|---|---|
| {assistant_offered} | {assistant shown or enabled for an eligible ticket — the denominator event} | {ticket_id:string · account_id:string(hashed) · channel:enum(email,chat)} | {named human} | {OM-1, GM-1} | planned |
| {ticket_deflected} | {assistant resolves ticket with no human handoff, at resolution commit} | {ticket_id:string · account_id:string(hashed) · channel:enum(email,chat) · model_version:string} | {named human} | {OM-1} | planned |

<!-- One row per event. Replace the example rows above once real rows exist.
     If a candidate row has nothing to put in Maps-to-metric, the row does not
     go in — that is the anti-bloat rule doing its job. -->

## Deleted events (graveyard)

| Event | Deleted | Why |
|---|---|---|
| {noisy_click_event} | {YYYY-MM-DD} | {no metric consumed it (VM-20)} |

<!-- Keep the graveyard. It is the only thing that stops a zombie event from
     being re-added next quarter by someone who was not in the room. -->
